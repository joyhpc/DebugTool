# A57 eDP Pilot Run

This directory is the canonical workspace for the A57 eDP rear two-channel debug case.

Current entry point:

- `latest-architecture-first.md` - current Architecture-First output for the case.

Archived prior outputs:

- `archive/input-cleaning-2026-05-08.md` - earlier input-cleaning-only record.
- `archive/architecture-first-initial-link-model.md` - earlier Architecture-First link-model record.

Maintenance rule:

- Keep one current entry point per case and mode.
- Move superseded outputs into `archive/` instead of leaving multiple same-case files in `pilot_runs/`.
- Do not overwrite archived records; create a new archive filename when preserving an older version.

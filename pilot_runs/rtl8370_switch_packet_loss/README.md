# RTL8370 Switch Packet-Loss Synthetic Dry Run

This directory is a synthetic dry-run used to exercise the generic failure matrix and same-window evidence batch templates.

It is not a reviewed real project case and must not be promoted to `case_record`, `signature`, `link_model`, regression, or training data without real evidence.

Current entries:

- `latest-input-cleaning.md` - cleaned natural-language intake.
- `latest-architecture-first.md` - Architecture-First debug analysis.
- `visual-architecture-brief.md` - first-page architecture view, mode gate, evidence stack, and field brief.
- `field-action-plan.md` - field execution plan using generic matrix and same-window evidence templates.

Reuse rule:

- Keep RTL8370 and switch-specific names only in this case directory.
- Do not hardcode this case into `forms/`, `output_contracts/`, validators, scripts, or reusable assets.

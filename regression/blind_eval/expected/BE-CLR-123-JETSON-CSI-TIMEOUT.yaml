case_id: BE-CLR-123-JETSON-CSI-TIMEOUT
source_record: training/closed_loop/synthetic_closures/CLR-123-NVIDIA-JETSON-CSI-MODE-SETTINGS.yaml
actual_resolution_summary: "NVIDIA Jetson guidance maps camera timeouts to mode-specific device-tree fields such as num_lanes, tegra_sinterface, discontinuous_clk, mclk_multiplier, pix_clk_hz, and lane_polarity, while I2C errors route to power_on order."
hit_criteria:
  must_include:
    - "device tree"
    - "num_lanes"
    - "tegra_sinterface"
    - "discontinuous_clk"
    - "mclk"
    - "pix_clk_hz"
    - "lane_polarity"
    - "power_on"
    - "I2C"
  must_not_include:
    - "userspace first"
    - "image processing first"
    - "rewrite capture app first"
  near_hit_min_fraction: 0.70
review_notes:
  - "A hit checks kernel mode settings, lane/clock fields, and sensor power/I2C before downstream image processing."

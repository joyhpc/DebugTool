# Reviewed Closed-Loop Records

This directory is reserved for human-reviewed, non-synthetic closed-loop records.

The historical CLR corpus is kept in `training/closed_loop/synthetic_closures/`
because the queue focus and source titles can leak the target lesson. Those
files are useful as training closures, but they are excluded from default
regression metrics.

# Release Notes — V0.9.2 Critical Fix

This release fixes the critical bug found in V0.9.1 review:

- link_model `use_when` fields were using their own titles rather than triggering symptoms.

It also adds stronger linting and the first output validator skeleton.

## Status

Founder-pilot candidate after local lint passes. Still not team-wide pilot ready.

## Next

Run one Fast Path case and one Architecture-First case, then use retrospective to update assets.
